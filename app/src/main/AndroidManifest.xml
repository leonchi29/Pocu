<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pocu"
        tools:targetApi="31">

        <!-- Activity de Login de Estudiante (INICIO) -->
        <activity
            android:name=".ui.StudentLoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Pocu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity para registro de dispositivo del estudiante -->
        <activity
            android:name=".ui.StudentRegisterDeviceActivity"
            android:exported="false"
            android:theme="@style/Theme.Pocu" />

        <!-- Activity Principal (después del login) -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Pocu" />

        <!-- Activity para configurar PIN -->
        <activity
            android:name=".ui.PinActivity"
            android:exported="false"
            android:theme="@style/Theme.Pocu" />

        <!-- Activity para configurar horarios -->
        <activity
            android:name=".ui.ScheduleActivity"
            android:exported="false"
            android:theme="@style/Theme.Pocu" />

        <!-- Activity para apps permitidas -->
        <activity
            android:name=".ui.AllowedAppsActivity"
            android:exported="false"
            android:theme="@style/Theme.Pocu" />

        <!-- Activity para configurar conexión web -->
        <activity
            android:name=".ui.WebSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.Pocu" />

        <!-- Activity para configurar servidor SQL -->
        <activity
            android:name=".ui.ServerSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.Pocu" />

        <!-- Activity para configurar Firebase (obsoleta - redirige a ServerSetupActivity) -->
        <activity
            android:name=".ui.FirebaseSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.Pocu" />

        <!-- Overlay de bloqueo -->
        <activity
            android:name=".ui.BlockerOverlayActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Pocu.Overlay" />

        <!-- Overlay de lockdown (bloqueo total) -->
        <activity
            android:name=".ui.LockdownOverlayActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Pocu.Overlay" />

        <!-- Servicio de bloqueo en foreground -->
        <service
            android:name=".service.AppBlockerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="appBlocking" />
        </service>

        <!-- Servicio de monitoreo de permisos -->
        <service
            android:name=".service.PermissionMonitorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="permissionMonitoring" />
        </service>

        <!-- Servicio de sincronización web -->
        <service
            android:name=".service.WebSyncService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="webSync" />
        </service>

        <!-- Servicio de sincronización SQL -->
        <service
            android:name=".service.SqlSyncService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="sqlSync" />
        </service>

        <!-- Accessibility Service para detectar apps -->
        <service
            android:name=".service.AppBlockerAccessibilityService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Device Admin Receiver para proteger desinstalación -->
        <receiver
            android:name=".admin.AppDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver para iniciar servicio al arrancar -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>